name: Legacy V0.8.2 manifest generator (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This legacy atomic workflow is retired."
          echo "Use generate_v082_all_reader_manifests_v2.yml, which validates and persists one paper at a time."
