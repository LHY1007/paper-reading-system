name: Generate all V0.8.2 manifests by sequential strong-AI review (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired path
        run: |
          echo "This workflow is retained only for audit history."
          echo "GitHub Models has been retired and the repository Copilot premium quota is exhausted."
          echo "Current reader manifests are built sequentially from exact PDF evidence, deterministic translation, source-audited paper-specific curation, and fail-closed validators."
          exit 1
